<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 包含第三方的Logback配置，但先处理这个配置文件中的设置 -->
    <include resource="org/winterfell/vintage/vertx/boot/logging/logback.xml"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 覆盖root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
    <!-- 其他自定义配置 -->

</configuration>